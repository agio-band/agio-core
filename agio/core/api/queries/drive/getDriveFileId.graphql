query GetFileID ($companyId: ID!, $filePath: String!){
  driveObjects(
    first: 1,
    filter:{
      where:{
        company: {
          id: {equalTo: $companyId}
        },
        path: {equalTo: $filePath}

      }
    }
  ){
    edges{
      node{
        id
      }
    }
  }
}