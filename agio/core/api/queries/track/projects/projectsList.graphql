query projectsList($first: Int!, $filter: ProjectFilter, $afterCursor: String) {
  projects(
    first: $first
    filter: $filter
    after: $afterCursor
  )
  {
    edges {
      node {
        id
        name
        code
        company {
          id
          name
          hostUser {
            id
          }
          logo(params: {type: "fit", width: 250, height: 250})
        }
        workspace {
          id
          name
        }
        workspaceRevision {
          id
          workspaceId
        }
      }
    }
     pageInfo {
        hasNextPage
        hasPreviousPage
        startCursor
        endCursor
    }
  }
}
