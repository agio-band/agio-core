query FindProject($filters: ProjectWhere){
  projects (
    filter: {
            where: $filters
        },
        first: 1,
  ){
    edges{
      node {
        id
        name
        code
        fields
        company {
          id
          name
          email
        }
        workspace {
          id
          name
        }
      }
    }
  }
}