query GetEntityList($where: EntityWhere, $first: Int!) {
    entities (
        filter: {
            where: $where
        },
        first: $first
    ){
        edges {
            node{
                id
                name
                class{
                    name
                }
                fields
                parentId
            }
        }

     pageInfo {
        hasNextPage
        hasPreviousPage
        startCursor
        endCursor
    }
    }
}