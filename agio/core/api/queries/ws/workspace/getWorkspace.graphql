query GetSingleWorkspaceById($id: ID!) {
    workspace(id: $id) {
        id
        name
        description
        createdAt
        updatedAt
        company {
            id
            name
            code
        }
        deletedAt
    }
}
