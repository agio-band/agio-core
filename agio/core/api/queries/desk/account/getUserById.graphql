query getCompanyById ($id: ID!) {
    user (id:$id) {
        id
        email
        firstName
        lastName
        avatar(params: {type: "fit", width: 480, height: 480})
        language {
            code
            name
            nativeName
        }
        groups {
            id
            name
        }
    }
}