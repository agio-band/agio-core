query getCompanyByCode($code: String!) {
  companies(
    first: 1,
    filter: {
      where: {
        code: {equalTo: $code}
      }
    }
  )
  {
    edges{
      node{
        id
        name
        code
        logo(params: {type: "fit", width: 480, height: 480})
      }
    }
  }
}