query getCompabyById ($first: Int!, $afterCursor: String) {
  companies(
      first: $first,
      after: $afterCursor
  )
  {
    edges{
      node{
        id
        name
        code
        logo(params: {type: "fit", width: 480, height: 480})
      }
    }
    pageInfo {
        hasNextPage
        hasPreviousPage
        startCursor
        endCursor
    }
  }
}