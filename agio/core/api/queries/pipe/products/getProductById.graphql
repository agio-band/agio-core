query GetProductById($id: ID!) {
    publish(id: $id) {
        id
        name
        type {
            id
            name
        }
        fields
        entityId
        variant
        fields
    }
}
