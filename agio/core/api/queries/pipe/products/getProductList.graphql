query GetProductList($first: Int!, $filter: PublishFilter) {
    publishes(
        first: $first,
        filter: $filter
    ) {
        edges {
            node {
                id
                name
                type
                fields
                variant
                entityId
            }
        }
        pageInfo {
            hasNextPage
            hasPreviousPage
            startCursor
            endCursor
        }
    }
}
