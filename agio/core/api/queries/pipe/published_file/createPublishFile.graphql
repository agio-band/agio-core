mutation CreatePublishFile($name: String!, $path: String!, $publishVersion: ID!)
{
  createPublishFile(input: {
    name: $name,
    path: $path,
    publishVersion: $publishVersion
  })
  {
    publishFileId
  }
}