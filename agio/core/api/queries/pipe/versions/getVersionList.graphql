query GetVersionList($first: Int!, $filter: PublishVersionFilter) {
    publishVersions(first: $first, filter: $filter) {
        edges {
            node {
                id
                name
                fields
                publish {
                    id
                    name
                    type
                }
                fields
            }
        }
        pageInfo {
            hasNextPage
            hasPreviousPage
            startCursor
            endCursor
        }
    }
}
