query GetPublishVersionsById($id: ID!) {
    publishVersion(id: $id) {
        id
        name
        fields
        publish {
            id
            name
            type {
                id
                name
            }
        }
        entity {
            id
            name
        }
        fields
    }
}
