query GetPublishSessionByID($id: ID!) {
  publishSession(id: $id) {
    id
    state
    status
    data
    entity {
      id
      name
    }
    publishVersions {
      id
      name
      publishId
    }
  }
}