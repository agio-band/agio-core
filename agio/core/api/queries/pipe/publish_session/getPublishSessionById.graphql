query GetPublishSessionByID($id: ID!) {
  publishSession(id: $id) {
    id
    name
    number
    state
    status
    data
    entity {
      id
      name
    }
    publishVersions {
      id
      name
      publishId
    }
  }
}